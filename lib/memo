class ImageSection extends StatelessWidget {
  const ImageSection({
    super.key,
     required this.image});

  final String image;


class ImageSection extends StatelessWidget {
  const ImageSection({
    super.key
    required this.image,
  });

  final String image;